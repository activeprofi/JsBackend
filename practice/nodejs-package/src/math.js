import cube from '../src/facade/numbers1';
import multi, { sum2 as sum, sub2 as sub, pow2 as pow } from '../src/facade/numbers2';
import sqrt from '../src/facade/numbers3';

export default cube;
export { sum, sub, sqrt, pow, multi };
